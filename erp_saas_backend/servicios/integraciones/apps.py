from django.apps import AppConfig


class IntegracionesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "servicios.integraciones"
    label = "integraciones"
    verbose_name = "Integraciones entre módulos"
