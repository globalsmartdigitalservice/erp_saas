from django.apps import AppConfig


class SeguridadConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "dominios.seguridad"
    label = "seguridad"
    verbose_name = "Seguridad"
